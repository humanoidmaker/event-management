# EventHub - Event Management

## Setup
```bash
cd backend && pip install -r requirements.txt && python -m scripts.seed_admin && uvicorn app.main:app --reload
cd frontend && npm install && npm run dev
```

## Login
- Admin: admin@event.local / admin123
